[pytest]
addopts = -rA -v --alluredir=reports/allure
markers =
    smoke: quick verification of core features (critical happy paths)
    regression: full regression suite across the application
    sanity: light regression for quick checks
    integration: tests that validate data consistency, system health, and core configurations 
     